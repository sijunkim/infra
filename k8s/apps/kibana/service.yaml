apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: prodesk
spec:
  # 클러스터 내부 전용 — 외부 노출은 IngressRoute(kibana.kimsijun.com)가 담당.
  type: ClusterIP
  selector:
    app: kibana
  ports:
  - port: 5601
    targetPort: 5601
