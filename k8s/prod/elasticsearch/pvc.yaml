apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elasticsearch-pvc
  namespace: dama-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: elasticsearch-prod-pv
  resources:
    requests:
      storage: 10Gi
