apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: dama-prod
spec:
  # 클러스터 내부 접근은 http://elasticsearch:9200(dama-api 등). NodePort 는 운영 점검용.
  type: ClusterIP
  selector:
    app: elasticsearch
  ports:
  - port: 9200
    targetPort: 9200
